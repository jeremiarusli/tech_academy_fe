export default {
  buildingStatus ({ status }) {
    return status
  },

  buildingMessage ({ message }) {
    return message
  },

  buildingData ({ buildings }) {
    return buildings
  },

  building ({ building }) {
    return building
  }
}
