export default {
  slotId ({ id }) {
    return id
  },

  slotStatus ({ status }) {
    return status
  },

  slotMessage ({ message }) {
    return message
  },

  slotData ({ slots }) {
    return slots
  }
}
